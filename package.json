{
  "name": "word-of-the-day",
  "homepage": "https://rational-kunal.github.io/Word-Of-The-Day",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "parcel watch manifest.json --config @parcel/config-webextension",
    "build": "rimraf dist && parcel build manifest.json --config @parcel/config-webextension",
    "build:gh": "rimraf dist && parcel build src/index.html --public-url ./",
    "predeploy": "npm run build:gh",
    "deploy": "gh-pages -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/config-webextension": "^2.1.1",
    "gh-pages": "^3.2.3",
    "parcel": "^2.1.1"
  },
  "dependencies": {
    "animejs": "^3.2.1",
    "rimraf": "^3.0.2"
  }
}
